<% unless current_user == user %>
  <% if current_user.following?(user) %>
    <%= button_to 'Unfollow', user_relationship_path(user.id), method: :delete %>
  <% else %>
    <%= button_to 'Follow', user_relationships_path(user.id) %>
  <% end %>
<% end %>